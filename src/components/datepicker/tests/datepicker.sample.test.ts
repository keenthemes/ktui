/**
 * Sample test for KTDatepicker
 *
 * This test verifies that the Vitest setup is working.
 */
import { describe, it, expect } from 'vitest';

describe('KTDatepicker sample', () => {
  it('should run a basic test', () => {
    expect(true).toBe(true);
  });
});